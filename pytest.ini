[pytest]
pythonpath = src
markers =
    unit: marks tests as unit tests
    integration: marks tests as integration tests
    allocation_required: marks tests that require an allocation
    requires_env(var): skip test if the given env var is not set
